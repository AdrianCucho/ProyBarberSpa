 
--## CLIENTES ##--
En la tabla de clientes se registrara a todas las personas, para poder utilizar sus datos como clientes o empleados(de ser el caso).

--## EMPLEADOS ## --
La tabla de empleados se crea en base a un cliente previamente registrado, este, contara con un tabla extra llamada "tb_perfil", para saber el perfil del empleado(admin, recepcion, barber).
Ademas, contara con una tabla adicional





--### USUARIOS ####--

Cada vez que se consiga un nuevo cliente, se va a tener que sacar una copia del script o de la bd de `pruebas y se va a copiar el poroyecto en php, se va a montar en el servidor y crearemos un usuario root en BD, con ese usuario, vamos a crear un usuario de tipo admin, que sera enviado via correo electronico, para que el usuariuo pueda habilitar el sistema y empiece a trabajar.

UserRoot -> Este usuario tendra los modulos de usuarioADM y ConfigPlanModeulos, donde el podra realizar el registro de usuarios admin para cada bd de cada usuario., tambien, de reehabilitar los modulos de acuerdo al plan que tiene actual.